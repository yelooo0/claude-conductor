# REVIEW of packet: <title>

Status: <APPROVED | ISSUES | ESCALATE>

## Verdict
_One line: is the packet complete per its acceptance checks? Did acceptance checks actually
run and pass in a fresh checkout of the worker's commit? If you did not run them, say so._

## Issues (numbered, one fix each, ordered by priority)
1. **severity: high|med|low** — location (`file:line`) — what is wrong and the concrete
   fix expected. Must be actionable without re-reading the whole system.
2. …

## Notes for planner
_If anything about this packet was underspecified or mis-scoped, say it here so the next
packet is better._

---
_Worker: fix the numbered issues only. Planner: if 2 worker rounds fail on the same packet,
escalate and take this packet over directly._